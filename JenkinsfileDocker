pipeline {
    agent any

    tools {
        org.jenkinsci.plugins.docker.commons.tools.DockerTool 'latest'
        nodejs 'latest'
    }

    stages {
        stage('Test') {
            steps {
                sh 'npm install'
                sh 'npm test'
            }
        }
    }
}
