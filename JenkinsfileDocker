pipeline {
    agent any

    tools {
        docker 'latest'
        nodejs 'latest'
    }

    stages {
        stage('Test') {
            steps {
                sh 'npm install'
                sh 'npm test'
            }
        }
    }
}
